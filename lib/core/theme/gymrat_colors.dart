import 'package:flutter/material.dart';

abstract final class GymRatColors {
  // Brand / workout
  static const Color green = Color(0xFF32D071);
  static const Color greenDark = Color(0xFF176B3D);

  // Rewards / progression
  static const Color gold = Color(0xFFFFC107);
  static const Color goldDark = Color(0xFF9A7000);

  // Premium
  static const Color premium = Color(0xFF9B6DFF);

  // Information / analytics
  static const Color info = Color(0xFF4C9FFF);

  // Errors / warnings
  static const Color danger = Color(0xFFFF5C5C);

  // Neutral surfaces
  static const Color black = Color(0xFF050606);
  static const Color background = Color(0xFF090B0C);
  static const Color surface = Color(0xFF111416);
  static const Color surfaceElevated = Color(0xFF181C1F);
  static const Color border = Color(0xFF272C30);

  // Text
  static const Color textPrimary = Color(0xFFF4F6F5);
  static const Color textSecondary = Color(0xFF9CA3A0);
  static const Color textMuted = Color(0xFF686F6C);
}
